{
  "name": "idle",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "agent:smoke": "pnpm --filter @idle/runtime build && node scripts/real-agent-local-smoke.mjs",
    "package:windows": "pnpm dlx esbuild apps/runtime/src/main.ts --bundle --platform=node --format=esm --outfile=apps/desktop/runtime-bundle/main.js && pnpm --filter @idle/desktop build && pnpm exec electron-builder --win nsis --projectDir apps/desktop --config electron-builder.yml"
  },
  "devDependencies": {
    "electron-builder": "26.15.3"
  }
}
